{
	"extends": "../../tsconfig.cjs.json",
	"compilerOptions": {
		// includes the package-root index.ts alongside src (preserves the previously inferred layout)
		"rootDir": ".",
		"outDir": "./dist/cjs"
	}
}